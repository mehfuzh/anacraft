/target
**/*.rs.bk
*.pdb

# Never commit OAuth material or captured credentials
client.json
*.secret.json
.env

# Byte-compiled helpers. The scripts/ tools are run with python3 directly,
# so these are a side effect of running them and are version-specific noise.
__pycache__/
*.pyc

# macOS finder droppings
.DS_Store

# Claude Code, personal to this checkout — permission grants and local overrides
.claude/settings.local.json
